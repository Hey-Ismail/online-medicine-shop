# Prevent PHP execution in the uploads directory (security hardening)
<FilesMatch "\.php$">
    Require all denied
</FilesMatch>

# Allow only image files
<FilesMatch "\.(jpg|jpeg|png|gif|webp)$">
    Require all granted
</FilesMatch>

Options -Indexes
